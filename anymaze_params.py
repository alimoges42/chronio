dims = (640, 480)                   # Dimensions of anymaze camera (in pixels)
mm_per_pixels = 0.8                 # Number of millimeters per pixel
pixels_per_mm = 1 / mm_per_pixels   # Number of pixels per millimeter


default_params = {
    'dims': dims,
    'mm_per_pixels': mm_per_pixels,
    'pixels_per_mm': pixels_per_mm
}
